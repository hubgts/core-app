# Variables d'environnement de PRODUCTION.
# Copier en docker/.env.prod et renseigner de vraies valeurs (NE PAS committer).
#   cp docker/.env.prod.example docker/.env.prod

# Base de données PostgreSQL
DB_USER=progression
DB_PASSWORD=change-moi-mot-de-passe-fort
DB_NAME=progression

# Port HTTP exposé sur l'hôte pour le frontend (nginx).
# Mettre 80 si l'app est exposée directement, ou laisser 8080 derrière
# un reverse-proxy hôte qui gère le TLS.
WEB_PORT=8080
